diff --git a/dist/index.js b/dist/index.js
index 87f02a8ee43ba88cee2280f5ee7b87893fff3a22..80efcc3fb88287d09edab3fe3c75fa00d08f7fb4 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,3 +1,4 @@
+// Modified by Linnya on 2026-09-12: share upstream user image encoding with native tool content.
 // src/deepseek-provider.ts
 import {
   NoSuchModelError
@@ -133,6 +134,98 @@ var supportedImageMediaTypes = /* @__PURE__ */ new Set([
   "image/png",
   "image/webp"
 ]);
+// Linnya patch: the upstream image codec is shared by native user/tool content.
+async function convertDeepSeekContentParts(content, prompt, providerOptionsName, warnings, role) {
+  const contentParts = [];
+  for (const part of content) {
+    if (part.type === "text") {
+      contentParts.push({ type: "text", text: part.text });
+    } else if (part.type === "file" && getTopLevelMediaType(part.mediaType) === "image") {
+      const filePartOptions = await parseProviderOptions({
+        provider: providerOptionsName,
+        providerOptions: part.providerOptions,
+        schema: deepseekFilePartProviderOptions
+      });
+      if (part.data.type === "reference") {
+        contentParts.push({
+          type: "file",
+          file_id: resolveProviderReference({
+            reference: part.data.reference,
+            provider: "deepseek"
+          })
+        });
+      } else if (part.data.type === "url" || part.data.type === "data") {
+        const resolvedMediaType = resolveFullMediaType({ part });
+        if (!supportedImageMediaTypes.has(resolvedMediaType)) {
+          throw new UnsupportedFunctionalityError({
+            functionality: `DeepSeek image media type ${resolvedMediaType}`,
+            message: "DeepSeek supports JPEG, PNG, GIF, and WebP image inputs."
+          });
+        }
+        if (part.data.type === "url") {
+          const url = part.data.url.toString();
+          if (url.length > 8192) {
+            throw new InvalidPromptError({
+              prompt,
+              message: "DeepSeek image URLs must not exceed 8192 characters."
+            });
+          }
+          if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
+            throw new InvalidPromptError({
+              prompt,
+              message: "DeepSeek `fileData` image parts require inline data, not a URL."
+            });
+          }
+          contentParts.push({
+            type: "image_url",
+            image_url: {
+              url,
+              ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
+                detail: filePartOptions.imageDetail
+              }
+            }
+          });
+        } else {
+          const dataUrl = `data:${resolvedMediaType === "image/jpg" ? "image/jpeg" : resolvedMediaType};base64,${convertToBase64(part.data.data)}`;
+          if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
+            if (filePartOptions.imageDetail != null) {
+              throw new InvalidPromptError({
+                prompt,
+                message: "DeepSeek `imageDetail` cannot be combined with `fileData`."
+              });
+            }
+            contentParts.push({
+              type: "file",
+              file_data: dataUrl,
+              ...part.filename != null && { filename: part.filename }
+            });
+          } else {
+            contentParts.push({
+              type: "image_url",
+              image_url: {
+                url: dataUrl,
+                ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
+                  detail: filePartOptions.imageDetail
+                }
+              }
+            });
+          }
+        }
+      } else {
+        warnings.push({
+          type: "unsupported",
+          feature: `${role} message part type: ${part.type}`
+        });
+      }
+    } else {
+      warnings.push({
+        type: "unsupported",
+        feature: `${role} message part type: ${part.type}`
+      });
+    }
+  }
+  return contentParts;
+}
 async function convertToDeepSeekChatMessages({
   prompt,
   responseFormat,
@@ -220,94 +313,9 @@ async function convertToDeepSeekChatMessages({
           });
           break;
         }
-        const userContent = [];
-        for (const part of content) {
-          if (part.type === "text") {
-            userContent.push({ type: "text", text: part.text });
-          } else if (part.type === "file" && getTopLevelMediaType(part.mediaType) === "image") {
-            const filePartOptions = await parseProviderOptions({
-              provider: providerOptionsName,
-              providerOptions: part.providerOptions,
-              schema: deepseekFilePartProviderOptions
-            });
-            if (part.data.type === "reference") {
-              userContent.push({
-                type: "file",
-                file_id: resolveProviderReference({
-                  reference: part.data.reference,
-                  provider: "deepseek"
-                })
-              });
-            } else if (part.data.type === "url" || part.data.type === "data") {
-              const resolvedMediaType = resolveFullMediaType({ part });
-              if (!supportedImageMediaTypes.has(resolvedMediaType)) {
-                throw new UnsupportedFunctionalityError({
-                  functionality: `DeepSeek image media type ${resolvedMediaType}`,
-                  message: "DeepSeek supports JPEG, PNG, GIF, and WebP image inputs."
-                });
-              }
-              if (part.data.type === "url") {
-                const url = part.data.url.toString();
-                if (url.length > 8192) {
-                  throw new InvalidPromptError({
-                    prompt,
-                    message: "DeepSeek image URLs must not exceed 8192 characters."
-                  });
-                }
-                if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
-                  throw new InvalidPromptError({
-                    prompt,
-                    message: "DeepSeek `fileData` image parts require inline data, not a URL."
-                  });
-                }
-                userContent.push({
-                  type: "image_url",
-                  image_url: {
-                    url,
-                    ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
-                      detail: filePartOptions.imageDetail
-                    }
-                  }
-                });
-              } else {
-                const dataUrl = `data:${resolvedMediaType === "image/jpg" ? "image/jpeg" : resolvedMediaType};base64,${convertToBase64(part.data.data)}`;
-                if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
-                  if (filePartOptions.imageDetail != null) {
-                    throw new InvalidPromptError({
-                      prompt,
-                      message: "DeepSeek `imageDetail` cannot be combined with `fileData`."
-                    });
-                  }
-                  userContent.push({
-                    type: "file",
-                    file_data: dataUrl,
-                    ...part.filename != null && { filename: part.filename }
-                  });
-                } else {
-                  userContent.push({
-                    type: "image_url",
-                    image_url: {
-                      url: dataUrl,
-                      ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
-                        detail: filePartOptions.imageDetail
-                      }
-                    }
-                  });
-                }
-              }
-            } else {
-              warnings.push({
-                type: "unsupported",
-                feature: `user message part type: ${part.type}`
-              });
-            }
-          } else {
-            warnings.push({
-              type: "unsupported",
-              feature: `user message part type: ${part.type}`
-            });
-          }
-        }
+        const userContent = await convertDeepSeekContentParts(
+          content, prompt, providerOptionsName, warnings, "user"
+        );
         messages.push({
           role: "user",
           content: userContent,
@@ -401,6 +409,10 @@ async function convertToDeepSeekChatMessages({
               contentValue = (_a = output.reason) != null ? _a : "Tool call execution denied.";
               break;
             case "content":
+              contentValue = await convertDeepSeekContentParts(
+                output.value, prompt, providerOptionsName, warnings, "tool"
+              );
+              break;
             case "json":
             case "error-json":
               contentValue = JSON.stringify(output.value);
@@ -1447,4 +1459,4 @@ export {
   deepSeek,
   deepSeek as deepseek
 };
-//# sourceMappingURL=index.js.map
\ No newline at end of file
+// The upstream source map predates the versioned Linnya patch; do not load stale mappings.
diff --git a/dist/internal/index.js b/dist/internal/index.js
index 0a1230c1cce71ac153fbae8ec8b64596593d2dc6..2e25fe158735803892b02c84b88771c481904320 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -1,3 +1,4 @@
+// Modified by Linnya on 2026-09-12: share upstream user image encoding with native tool content.
 // src/chat/deepseek-chat-language-model.ts
 import {
   combineHeaders,
@@ -123,6 +124,98 @@ var supportedImageMediaTypes = /* @__PURE__ */ new Set([
   "image/png",
   "image/webp"
 ]);
+// Linnya patch: the upstream image codec is shared by native user/tool content.
+async function convertDeepSeekContentParts(content, prompt, providerOptionsName, warnings, role) {
+  const contentParts = [];
+  for (const part of content) {
+    if (part.type === "text") {
+      contentParts.push({ type: "text", text: part.text });
+    } else if (part.type === "file" && getTopLevelMediaType(part.mediaType) === "image") {
+      const filePartOptions = await parseProviderOptions({
+        provider: providerOptionsName,
+        providerOptions: part.providerOptions,
+        schema: deepseekFilePartProviderOptions
+      });
+      if (part.data.type === "reference") {
+        contentParts.push({
+          type: "file",
+          file_id: resolveProviderReference({
+            reference: part.data.reference,
+            provider: "deepseek"
+          })
+        });
+      } else if (part.data.type === "url" || part.data.type === "data") {
+        const resolvedMediaType = resolveFullMediaType({ part });
+        if (!supportedImageMediaTypes.has(resolvedMediaType)) {
+          throw new UnsupportedFunctionalityError({
+            functionality: `DeepSeek image media type ${resolvedMediaType}`,
+            message: "DeepSeek supports JPEG, PNG, GIF, and WebP image inputs."
+          });
+        }
+        if (part.data.type === "url") {
+          const url = part.data.url.toString();
+          if (url.length > 8192) {
+            throw new InvalidPromptError({
+              prompt,
+              message: "DeepSeek image URLs must not exceed 8192 characters."
+            });
+          }
+          if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
+            throw new InvalidPromptError({
+              prompt,
+              message: "DeepSeek `fileData` image parts require inline data, not a URL."
+            });
+          }
+          contentParts.push({
+            type: "image_url",
+            image_url: {
+              url,
+              ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
+                detail: filePartOptions.imageDetail
+              }
+            }
+          });
+        } else {
+          const dataUrl = `data:${resolvedMediaType === "image/jpg" ? "image/jpeg" : resolvedMediaType};base64,${convertToBase64(part.data.data)}`;
+          if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
+            if (filePartOptions.imageDetail != null) {
+              throw new InvalidPromptError({
+                prompt,
+                message: "DeepSeek `imageDetail` cannot be combined with `fileData`."
+              });
+            }
+            contentParts.push({
+              type: "file",
+              file_data: dataUrl,
+              ...part.filename != null && { filename: part.filename }
+            });
+          } else {
+            contentParts.push({
+              type: "image_url",
+              image_url: {
+                url: dataUrl,
+                ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
+                  detail: filePartOptions.imageDetail
+                }
+              }
+            });
+          }
+        }
+      } else {
+        warnings.push({
+          type: "unsupported",
+          feature: `${role} message part type: ${part.type}`
+        });
+      }
+    } else {
+      warnings.push({
+        type: "unsupported",
+        feature: `${role} message part type: ${part.type}`
+      });
+    }
+  }
+  return contentParts;
+}
 async function convertToDeepSeekChatMessages({
   prompt,
   responseFormat,
@@ -210,94 +303,9 @@ async function convertToDeepSeekChatMessages({
           });
           break;
         }
-        const userContent = [];
-        for (const part of content) {
-          if (part.type === "text") {
-            userContent.push({ type: "text", text: part.text });
-          } else if (part.type === "file" && getTopLevelMediaType(part.mediaType) === "image") {
-            const filePartOptions = await parseProviderOptions({
-              provider: providerOptionsName,
-              providerOptions: part.providerOptions,
-              schema: deepseekFilePartProviderOptions
-            });
-            if (part.data.type === "reference") {
-              userContent.push({
-                type: "file",
-                file_id: resolveProviderReference({
-                  reference: part.data.reference,
-                  provider: "deepseek"
-                })
-              });
-            } else if (part.data.type === "url" || part.data.type === "data") {
-              const resolvedMediaType = resolveFullMediaType({ part });
-              if (!supportedImageMediaTypes.has(resolvedMediaType)) {
-                throw new UnsupportedFunctionalityError({
-                  functionality: `DeepSeek image media type ${resolvedMediaType}`,
-                  message: "DeepSeek supports JPEG, PNG, GIF, and WebP image inputs."
-                });
-              }
-              if (part.data.type === "url") {
-                const url = part.data.url.toString();
-                if (url.length > 8192) {
-                  throw new InvalidPromptError({
-                    prompt,
-                    message: "DeepSeek image URLs must not exceed 8192 characters."
-                  });
-                }
-                if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
-                  throw new InvalidPromptError({
-                    prompt,
-                    message: "DeepSeek `fileData` image parts require inline data, not a URL."
-                  });
-                }
-                userContent.push({
-                  type: "image_url",
-                  image_url: {
-                    url,
-                    ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
-                      detail: filePartOptions.imageDetail
-                    }
-                  }
-                });
-              } else {
-                const dataUrl = `data:${resolvedMediaType === "image/jpg" ? "image/jpeg" : resolvedMediaType};base64,${convertToBase64(part.data.data)}`;
-                if ((filePartOptions == null ? void 0 : filePartOptions.fileData) === true) {
-                  if (filePartOptions.imageDetail != null) {
-                    throw new InvalidPromptError({
-                      prompt,
-                      message: "DeepSeek `imageDetail` cannot be combined with `fileData`."
-                    });
-                  }
-                  userContent.push({
-                    type: "file",
-                    file_data: dataUrl,
-                    ...part.filename != null && { filename: part.filename }
-                  });
-                } else {
-                  userContent.push({
-                    type: "image_url",
-                    image_url: {
-                      url: dataUrl,
-                      ...(filePartOptions == null ? void 0 : filePartOptions.imageDetail) != null && {
-                        detail: filePartOptions.imageDetail
-                      }
-                    }
-                  });
-                }
-              }
-            } else {
-              warnings.push({
-                type: "unsupported",
-                feature: `user message part type: ${part.type}`
-              });
-            }
-          } else {
-            warnings.push({
-              type: "unsupported",
-              feature: `user message part type: ${part.type}`
-            });
-          }
-        }
+        const userContent = await convertDeepSeekContentParts(
+          content, prompt, providerOptionsName, warnings, "user"
+        );
         messages.push({
           role: "user",
           content: userContent,
@@ -391,6 +399,10 @@ async function convertToDeepSeekChatMessages({
               contentValue = (_a = output.reason) != null ? _a : "Tool call execution denied.";
               break;
             case "content":
+              contentValue = await convertDeepSeekContentParts(
+                output.value, prompt, providerOptionsName, warnings, "tool"
+              );
+              break;
             case "json":
             case "error-json":
               contentValue = JSON.stringify(output.value);
@@ -1179,4 +1191,4 @@ export {
   deepseekLanguageModelChatOptions,
   deepseekMessageProviderOptions
 };
-//# sourceMappingURL=index.js.map
\ No newline at end of file
+// The upstream source map predates the versioned Linnya patch; do not load stale mappings.
diff --git a/src/chat/convert-to-deepseek-chat-messages.ts b/src/chat/convert-to-deepseek-chat-messages.ts
index 3773339a0c5a0c5a7efa9a2054152e571256d33b..0a67b88e1cf01cb53c33334753c8d7481bf37898 100644
--- a/src/chat/convert-to-deepseek-chat-messages.ts
+++ b/src/chat/convert-to-deepseek-chat-messages.ts
@@ -1,8 +1,10 @@
+// Modified by Linnya on 2026-09-12: share upstream user image encoding with native tool content.
 import {
   InvalidPromptError,
   UnsupportedFunctionalityError,
   type LanguageModelV4CallOptions,
   type LanguageModelV4Prompt,
+  type LanguageModelV4ToolResultOutput,
   type SharedV4Warning,
 } from '@ai-sdk/provider';
 import {
@@ -28,6 +30,124 @@ const supportedImageMediaTypes = new Set([
   'image/webp',
 ]);
 
+async function convertDeepSeekContentParts(
+  content: Extract<LanguageModelV4ToolResultOutput, { type: 'content' }>['value'],
+  prompt: LanguageModelV4Prompt,
+  providerOptionsName: string,
+  warnings: Array<SharedV4Warning>,
+  role: 'user' | 'tool',
+): Promise<Array<DeepSeekContentPart>> {
+  const contentParts: Array<DeepSeekContentPart> = [];
+  for (const part of content) {
+    if (part.type === 'text') {
+      contentParts.push({ type: 'text', text: part.text });
+    } else if (
+      part.type === 'file' &&
+      getTopLevelMediaType(part.mediaType) === 'image'
+    ) {
+      const filePartOptions = await parseProviderOptions({
+        provider: providerOptionsName,
+        providerOptions: part.providerOptions,
+        schema: deepseekFilePartProviderOptions,
+      });
+
+      if (part.data.type === 'reference') {
+        contentParts.push({
+          type: 'file',
+          file_id: resolveProviderReference({
+            reference: part.data.reference,
+            provider: 'deepseek',
+          }),
+        });
+      } else if (part.data.type === 'url' || part.data.type === 'data') {
+        const resolvedMediaType = resolveFullMediaType({ part });
+
+        if (!supportedImageMediaTypes.has(resolvedMediaType)) {
+          throw new UnsupportedFunctionalityError({
+            functionality: `DeepSeek image media type ${resolvedMediaType}`,
+            message:
+              'DeepSeek supports JPEG, PNG, GIF, and WebP image inputs.',
+          });
+        }
+
+        if (part.data.type === 'url') {
+          const url = part.data.url.toString();
+
+          if (url.length > 8192) {
+            throw new InvalidPromptError({
+              prompt,
+              message:
+                'DeepSeek image URLs must not exceed 8192 characters.',
+            });
+          }
+
+          if (filePartOptions?.fileData === true) {
+            throw new InvalidPromptError({
+              prompt,
+              message:
+                'DeepSeek `fileData` image parts require inline data, not a URL.',
+            });
+          }
+
+          contentParts.push({
+            type: 'image_url',
+            image_url: {
+              url,
+              ...(filePartOptions?.imageDetail != null && {
+                detail: filePartOptions.imageDetail,
+              }),
+            },
+          });
+        } else {
+          const dataUrl = `data:${
+            resolvedMediaType === 'image/jpg'
+              ? 'image/jpeg'
+              : resolvedMediaType
+          };base64,${convertToBase64(part.data.data)}`;
+
+          if (filePartOptions?.fileData === true) {
+            if (filePartOptions.imageDetail != null) {
+              throw new InvalidPromptError({
+                prompt,
+                message:
+                  'DeepSeek `imageDetail` cannot be combined with `fileData`.',
+              });
+            }
+
+            contentParts.push({
+              type: 'file',
+              file_data: dataUrl,
+              ...(part.filename != null && { filename: part.filename }),
+            });
+          } else {
+            contentParts.push({
+              type: 'image_url',
+              image_url: {
+                url: dataUrl,
+                ...(filePartOptions?.imageDetail != null && {
+                  detail: filePartOptions.imageDetail,
+                }),
+              },
+            });
+          }
+        }
+      } else {
+        warnings.push({
+          type: 'unsupported',
+          feature: `${role} message part type: ${part.type}`,
+        });
+      }
+    } else {
+      warnings.push({
+        type: 'unsupported',
+        feature: `${role} message part type: ${part.type}`,
+      });
+    }
+  }
+
+  return contentParts;
+}
+
 export async function convertToDeepSeekChatMessages({
   prompt,
   responseFormat,
@@ -146,113 +266,9 @@ export async function convertToDeepSeekChatMessages({
           break;
         }
 
-        const userContent: Array<DeepSeekContentPart> = [];
-        for (const part of content) {
-          if (part.type === 'text') {
-            userContent.push({ type: 'text', text: part.text });
-          } else if (
-            part.type === 'file' &&
-            getTopLevelMediaType(part.mediaType) === 'image'
-          ) {
-            const filePartOptions = await parseProviderOptions({
-              provider: providerOptionsName,
-              providerOptions: part.providerOptions,
-              schema: deepseekFilePartProviderOptions,
-            });
-
-            if (part.data.type === 'reference') {
-              userContent.push({
-                type: 'file',
-                file_id: resolveProviderReference({
-                  reference: part.data.reference,
-                  provider: 'deepseek',
-                }),
-              });
-            } else if (part.data.type === 'url' || part.data.type === 'data') {
-              const resolvedMediaType = resolveFullMediaType({ part });
-
-              if (!supportedImageMediaTypes.has(resolvedMediaType)) {
-                throw new UnsupportedFunctionalityError({
-                  functionality: `DeepSeek image media type ${resolvedMediaType}`,
-                  message:
-                    'DeepSeek supports JPEG, PNG, GIF, and WebP image inputs.',
-                });
-              }
-
-              if (part.data.type === 'url') {
-                const url = part.data.url.toString();
-
-                if (url.length > 8192) {
-                  throw new InvalidPromptError({
-                    prompt,
-                    message:
-                      'DeepSeek image URLs must not exceed 8192 characters.',
-                  });
-                }
-
-                if (filePartOptions?.fileData === true) {
-                  throw new InvalidPromptError({
-                    prompt,
-                    message:
-                      'DeepSeek `fileData` image parts require inline data, not a URL.',
-                  });
-                }
-
-                userContent.push({
-                  type: 'image_url',
-                  image_url: {
-                    url,
-                    ...(filePartOptions?.imageDetail != null && {
-                      detail: filePartOptions.imageDetail,
-                    }),
-                  },
-                });
-              } else {
-                const dataUrl = `data:${
-                  resolvedMediaType === 'image/jpg'
-                    ? 'image/jpeg'
-                    : resolvedMediaType
-                };base64,${convertToBase64(part.data.data)}`;
-
-                if (filePartOptions?.fileData === true) {
-                  if (filePartOptions.imageDetail != null) {
-                    throw new InvalidPromptError({
-                      prompt,
-                      message:
-                        'DeepSeek `imageDetail` cannot be combined with `fileData`.',
-                    });
-                  }
-
-                  userContent.push({
-                    type: 'file',
-                    file_data: dataUrl,
-                    ...(part.filename != null && { filename: part.filename }),
-                  });
-                } else {
-                  userContent.push({
-                    type: 'image_url',
-                    image_url: {
-                      url: dataUrl,
-                      ...(filePartOptions?.imageDetail != null && {
-                        detail: filePartOptions.imageDetail,
-                      }),
-                    },
-                  });
-                }
-              }
-            } else {
-              warnings.push({
-                type: 'unsupported',
-                feature: `user message part type: ${part.type}`,
-              });
-            }
-          } else {
-            warnings.push({
-              type: 'unsupported',
-              feature: `user message part type: ${part.type}`,
-            });
-          }
-        }
+        const userContent = await convertDeepSeekContentParts(
+          content, prompt, providerOptionsName, warnings, 'user',
+        );
 
         messages.push({
           role: 'user',
@@ -357,7 +373,7 @@ export async function convertToDeepSeekChatMessages({
           }
           const output = toolResponse.output;
 
-          let contentValue: string;
+          let contentValue: string | Array<DeepSeekContentPart>;
           switch (output.type) {
             case 'text':
             case 'error-text':
@@ -367,6 +383,11 @@ export async function convertToDeepSeekChatMessages({
               contentValue = output.reason ?? 'Tool call execution denied.';
               break;
             case 'content':
+              // Linnya patch: preserve native tool image parts instead of serializing bytes as text.
+              contentValue = await convertDeepSeekContentParts(
+                output.value, prompt, providerOptionsName, warnings, 'tool',
+              );
+              break;
             case 'json':
             case 'error-json':
               contentValue = JSON.stringify(output.value);
diff --git a/src/chat/deepseek-chat-api-types.ts b/src/chat/deepseek-chat-api-types.ts
index 3a9c4963976566a4cb7a85c782c930fe4e2aade6..f01616e655deb6a4887cce34d017b1df3649dbd3 100644
--- a/src/chat/deepseek-chat-api-types.ts
+++ b/src/chat/deepseek-chat-api-types.ts
@@ -1,3 +1,4 @@
+// Modified by Linnya on 2026-09-12: share upstream user image encoding with native tool content.
 import { lazySchema, zodSchema } from '@ai-sdk/provider-utils';
 import { z } from 'zod/v4';
 
@@ -70,7 +71,7 @@ export interface DeepSeekMessageToolCall {
 
 export interface DeepSeekToolMessage {
   role: 'tool';
-  content: string;
+  content: string | Array<DeepSeekContentPart>;
   tool_call_id: string;
 }
 
