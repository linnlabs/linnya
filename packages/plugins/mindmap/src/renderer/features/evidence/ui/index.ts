/**
 * Evidence Feature UI exports
 *
 * 中文说明：
 * - 当前 UI 交互已收敛为：节点 addons（ReferenceAddon，经 NodeAddonsHost 统一 Teleport）+ 插入引用面板（ReferenceInsertPanel）
 * - 旧的侧边栏/徽章/快速预览方案已清理
 */

export { default as ReferenceInsertPanel } from './ReferenceInsertPanel.vue'
export { default as ReferenceAddon } from './ReferenceAddon.vue'

