# Slides limited manual editing

`manualEditing` owns the Renderer side of limited editing for generated `deck.js` presentations. It provides compiler-projected author targets and temporary visual projections, and submits a strict command against the exact revision/source snapshot. The backend remains the only writer. It may use the proven post-layout translation projection for a unique top-level atomic object; all other edits use the full author compiler. Both paths commit the semantic revision without waiting for PPTX assembly and publish the same revision contract.

## Current interaction

- The toolbar edit button is available only for a ready generated document whose visible RenderModel version equals the build-state revision and whose current slide contains at least one editable author object.
- The slide background is not an author target. Manual hover resolves the same projected target path used by pointer-down: movable objects show a move cursor, selectable-only objects show a pointer, and empty slide background keeps the default cursor. Clicking that empty background clears the current selection.
- Clicking an object resolves one author hierarchy path from the compiler-projected `authoringAncestorRefs`. The first click selects the outermost visible Frame; another click at the same child location enters the next layer. Once a child is selected, one click on a sibling selects that sibling directly; a click into a different nested branch stops at the first target after the common ancestor. The selection outline alone identifies the current target; no hierarchy label or author-key breadcrumb is rendered. Pointer-down locks the current layer as the gesture owner, so dragging over a child moves the selected Frame rather than switching targets mid-gesture. Releasing promotes the delta to a local pending translation before sending one `translate_by` operation in inches. The pending visual is removed only after the committed RenderModel revision and its current-page image/chart resources form the displayed frame, or rolled back on failure.
- Double-clicking a text-only Text (plain or rich) or plain-string Shape enters [`editingInteraction`](../editingInteraction/README.md), the sole owner of input, IME and interaction handoff. The sibling [`textEditing`](../textEditing/README.md) provides the native DOM view. Clicking another object ends input and selects it immediately while a passive preview retains the submitted text. Submission and frame settlement never hold or reclaim input focus. Multiline and mixed-font strings remain editable when the compiler declares a plain author string.
- After selecting an author object, `Backspace` or `Delete` submits one typed `delete_target` intent. Atomic text, shape, image, table, chart, SVG and formula targets remove only themselves; a Frame removes its complete author subtree. The Stage takes keyboard focus when the user selects on the canvas, but never intercepts deletion from input, textarea, select, button or contenteditable controls. An active inline text session retains ownership of both keys.
- Selecting an object exposes the sibling [`elementProperties`](../elementProperties/README.md) feature only when the compiler projects at least one visible property action: plain-text font size/color, Frame/Shape solid color, Shape/Image visual width/height, or deletion of any author target. The compact toolbar follows the current selected bounds and opens color/size controls on demand; the object toolbar hides during dragging, resizing and inline text input without changing the canvas viewport. A non-empty Text range opens the same toolbar in text-selection scope, emitting local style patches rather than object commands. Deletion-only targets receive the same icon button; the button and keyboard use the same `editingInteraction.deleteSelectedTarget` entry. A truly translate-only target has no property toolbar, and the toolbar does not repeat an author key as a floating title.
- Text-only rich author runs enter the same input session and save complete styled content. Formula-containing author runs remain text-read-only because this editor cannot preserve editable formula structure; they may still move.
- Image and Shape author objects may resize when projected as safe; Table, Chart, SVG Graphic and formula author objects currently only move. Their content/data/source editors remain later independent feature slices.
- Resizable Shape/Image selections expose all four corners and four edge midpoints. A small round marker sits inside a larger transparent pointer target. Each gesture keeps the opposite corner or edge midpoint fixed; shapes adjust independent dimensions and images preserve their current aspect ratio (side dragging expands the other axis symmetrically). `set_visual_size` carries the final dimensions and optional anchor-compensation `translationDelta` in one atomic edit. Rotated targets project pointer displacement onto their local axes, with the same zoom conversion as the stage. The gesture starts from the currently presented geometry, including queued edits. Pointer capture keeps dragging active outside the handle; local Canvas, selection and property previews update together, with no per-move IPC. Release promotes exactly one final operation into the existing queue before removing the transient preview. Inner text synchronously reflows with the preview box using the shared text-layout algorithm and backend-prepared shaping/metrics facts. Default font size, padding, wrapping and autofit use the same rules as the committed model, so installing the revision does not introduce a second text-size change. No platform measurement or compilation runs per pointer move; snapshots live with their RenderModel revision. Nodes without sufficient prepared facts retain the complete prior node until the formal revision; they never approximate new text layout by moving old lines. Escape/pointer cancellation clears only the current gesture; a click or return to the original size submits nothing. Completion returns keyboard focus to the stage, preserving Delete/Backspace. Accessible numeric inputs remain the keyboard resizing path. Frame resizing remains outside the projected capability contract. Numeric property edits retain the current top-left origin.
- A decorated Flex Frame is selected through its background and moves as one author object. The compiler projects every flattened descendant's `authoringAncestorRefs`; the target mapper turns that relation into the exact RenderNode roots shared by optimistic translation, deletion and parent selection geometry. The parent outline is the world-coordinate union of the visible roots, so a child moved beyond the Frame background remains inside the parent selection. Hit testing still uses each real object polygon rather than the union rectangle, so empty space between members does not become clickable. Active and queued child previews feed the same union before compilation finishes. Child text and visual objects keep their own author identity, so clicking them can still select and edit the child independently. Nested rendered Groups contribute only their outer affected root, avoiding a double transform.

The existing source-selection/AI-edit mode and manual-edit mode are mutually exclusive. Both consume compiler facts from the same RenderModel and share the same pointer-to-slide coordinate function. Source selection may admit any node with source ownership, while manual editing additionally requires an explicit author identity and capability; their transient selections and write workflows remain in separate feature stores.

## Layers

```text
definitions/
  manualEditingTypes + localized message catalog
functions/
  author-capability target/path projection, hit testing, resize geometry, availability, delete command/shortcut rules, command creation, result messages
orchestration/
  captured resize gesture, serial submit/refresh/receipt workflow
store/
  mode, selected target, gesture preview, queued entries and discriminated submission state (synchronous writes only)
ui/
  localization adapter and resize handles
```

The store never calls IPC and never contains geometry or conflict rules. Generic world-coordinate traversal belongs to the sibling `renderNodeSelection` feature; manual editing contributes only its author-capability predicate and target mapping. `SlidesView` supplies snapshot, IPC and refresh ports to `useManualEditQueue` and provides its narrow submission port to the Stage. The queue owns coalescing, dispatch and explicit ticket settlement; stores only receive synchronous state writes. `submitManualEdit` owns exact-command creation and idempotent transport retry, and returns the backend result before any visual refresh. `flush()` waits for every admitted backend write, rejects save/dependency failures, and does not require the final visual frame; export and Host navigation consume it through documentRuntime. Render-model errors unblock the save barrier with an explicit refresh failure instead of leaving it pending forever. The next intent starts only after the prior committed revision is both the current build version and the installed RenderModel version. `SlideStage` records presentation only when `renderVisualResources` has atomically installed the target page frame. Active, queued and in-progress resize previews share one projection collector for painting, hit testing and passive text geometry, so the canvas does not jump back and a moved or resized target remains selectable at its visible position while compilation catches up. Adjacent queued size edits retain the last dimensions and accumulate every anchor translation; selection and content previews apply the same sequence. Other adjacent edits of the same kind and author target are coalesced; for example, a font-size change followed quickly by a color change becomes one `set_text_style` compile.

## Conflict and failure behavior

- A stale revision/source snapshot refreshes the document and explains that another operation won.
- An unresolved AI draft refreshes into the existing draft failure screen; manual editing never deletes it.
- Source validation or full-build failure keeps the current compiled revision visible, returns failed to the active ticket and blocked to every dependent queued ticket. Temporary non-text previews roll back; editingInteraction retains corresponding text drafts for recovery. The UI explains that subsequent edits were not submitted. Selecting another object does not clear the failure summary.
- While a command or its complete visual frame is pending, the committed frame remains selectable and property, text, and drag gestures append typed intents with immediate previews. Exact-revision submission remains serialized, so the backend never receives a command against a stale base.
- A committed response is an accepted source revision, not proof that the new pixels are visible. The feature keeps `pendingPresentationRevision` until the matching or newer complete visual frame reaches the stage. It also handles the inverse race where that frame arrives before the command response.
- Command refresh and `workspace.document.updated(version)` refresh carry the target revision into one coalesced read. A completed duplicate is skipped, and a late request for a previously open document cannot reactivate or overwrite the current document.
- If the first IPC response is lost, the workflow retries once with the same command ID; the backend receipt returns the already committed revision instead of creating a duplicate.
- A committed write followed by refresh failure stays awaiting_frame, preserves its preview, and exposes a refresh-only retry; it is never reported as a failed write or submitted again.
- Each admission has a clientOperationId; merged entries retain every identity and bind them to the first ID as the backend commandId. A resolved ticket is durable even when response and presentation happen in one event-loop turn. Document generations reject late results, including A→B→A navigation.
- The Host file-session save barrier drains admitted edits before document switch/close; only then does the switch reset feature state. A RenderModel revision update reconciles selection by stable render ID.

## Tests

- `functions/manualEditableTargets.test.ts`: explicit author capabilities, topmost and projected-preview hit testing, parent-first Frame paths, source-span independence, flattened Frame translation scope, outlying-child selection union without empty-gap hit expansion, queued deletion exclusion, locked/rich/formula text boundaries.
- `functions/manualEditingAvailability.test.ts`: generated/ready/exact-version gate.
- `orchestration/submitManualEdit.test.ts`: exact command snapshot, transport retry and unavailable snapshots; refresh is tested with the real queue in editingInteraction.
- `functions/resolveManualClickSelection.test.ts`: parent-first descent, direct sibling switching, nested branch boundaries and unrelated paths.
- `functions/appendManualEditIntent.test.ts`: adjacent style and translation coalescing without crossing author targets.
- `../editingInteraction/orchestration/useSlideEditingInteraction.test.ts`: target-only hover and background deselection, drag-to-pending promotion, parent-first repeated-click descent, sibling switching during a style revision, stable Frame drag ownership, queued interaction during compilation, atomic deletion and immediate selection during pending text submission.
- `functions/manualEditingCursor.test.ts`: default background, selectable-only and movable-target cursor semantics.
- `functions/manualResize.test.ts`: all eight opposite anchors for rotated shapes and images, zoomed resizing, a second resize from pending geometry, image aspect ratio, minimum dimensions and no-op/capability boundaries.
- `functions/createManualDeleteOperation.test.ts` and `functions/manualDeleteShortcut.test.ts`: capability-bound target-kind preservation and native editing-control keyboard ownership.
- `functions/manualVisualPreview.test.ts`: complete-frame deferral without measurement facts, fill/size/deletion projection and parent outline expansion. Node derivation and authoritative preview geometry belong to [editingPreview](../editingPreview/README.md); selection no longer runs a separate resize algorithm.
- `../editingInteraction/orchestration/editingWorkflow.test.ts`: real queue serialization, receipt identity, coalescing, frame settlement, refresh failure and document-switch races.
- `features/elementProperties`: capability-bound command construction, move-only toolbar suppression, pane-local placement and property-toolbar assembly.
- `features/textEditing`: committed text projection and zoomed DOM geometry; session behavior belongs to editingInteraction.
- `page/SlidesView.test.ts`: preloaded surface mount followed by first/second save, plus component-level command submission, post-commit refresh and next-intent dispatch against the newly presented exact revision.
- Backend orchestration, IPC parsing, source rewrite, CAS, draft protection and receipt tests remain in `backend/features/presentationManualEditing`, `backend/ipc` and persistence suites.
- `smoke:preview-transitions` mounts the production `KonvaSlideStage` and verifies that one Frame preview delta moves multiple related content nodes in the same real Chromium frame. On one persistent Shape node it also checks fill, visual size, Frame-delete hiding and A→B→A restoration, in addition to the existing persistent-paint pixel comparisons.
- The same smoke mounts production `ManualResizeHandles` and `ElementPropertyToolbar`; Electron native pointer input verifies intermediate preview without submission, one submission per release, all eight native drag directions including consecutive left/top resizes before revision installation, Escape rollback, click-without-drag and keyboard focus restoration. It saves theme screenshots under `dist/dev/manual-properties-*.png`. Localization uses fixture fallback text; no user database or backend write is involved.

## Performance trace

Each accepted command uses its UUID as the Renderer/Backend correlation ID. When
`localStorage['linnya.slides.debug']` is set to `verbose`, the Renderer emits structured
`Slides/ManualEditTrace` events for request start, retry, response, refresh completion and the first
browser animation frame after the complete target revision is installed. The terminal
`frame_presented` event includes input-to-response, response-to-refresh, refresh-to-frame and total
input-to-frame timings. Backend `slides_manual_edit.backend_trace` events use the same command ID and
identify `projected_translation` versus `full_compile` without logging source or edited content.

- `backend/__tests__/manual-edit-save-reopen.integration.test.ts` joins the production Renderer queue, sandbox profile/evaluator, compiler, manual runtime, disk SQLite repository and lazy PPTX artifact runtime. It edits Shape text, position and color in sequence, reopens the database, and verifies the resulting source, revision, DeckSpec and exported XML. Host IPC transport and native UI are covered separately; the test does not touch user documents.

## Ordered text author-value previews

Text-content intents carry a typed visual preview alongside style operations, retaining their exact queue order through coalescing and frame settlement. `projectTextEditingValues` projects full content, whole-run fields and inherited baseline together; `createPresentedTextEditingTarget` combines those values with the existing geometry projection. Property controls and input/draft presentation consume this narrow public function rather than implementing different rich-text precedence. Canvas still draws pending content through `TextDraftPreview`; adding content to the ordered preview does not introduce another text layout engine or persistent document.

## Complete visual projection

[editingPreview](../editingPreview/README.md) owns paint and layout projection. Canvas, selection, hit testing and editor re-entry consume the same complete nodes. The queue only owns ordered intents and revision settlement; manualEditing no longer owns a partial run-style projection or a Shape-only old-line repositioner. A text-size operation updates layout and box together when exact local facts are available. Flow/intrinsic author layout and missing measurements retain the complete prior frame until formal compilation. A weak cache shares the synchronous result without adding a second store or asynchronous callback lifecycle.
