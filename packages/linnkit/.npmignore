# `@linnlabs/linnkit` publish whitelist 已通过 package.json#files 控制：
#   dist + LICENSE + CHANGELOG.md + README.md + README.zh-CN.md + docs/README.md + docs/integration/
# 这里再加一道兜底黑名单，避免误把开发期文件 / 不发布的 docs 子集 打进 tarball。
# 任何变动必须同时同步 packages/linnkit/__tests__/package.shell.test.ts 中的 files 断言。
#
# 公开包只发布长期稳定的接入文档。一次性计划、内部 runbook、
# 历史档案和维护方专用文档都不进入 tarball。
# 外部接入方需要的所有内容都在 README / CHANGELOG / docs/integration/。

# 源码本体：以 src/index.ts 形态运行的代码不发布；外部消费者一律从 dist/ 引用
src/

# docs/ 中不发包给消费者的子集（兜底——files 白名单已不带这些）
docs/framework/
docs/archive/
docs/99-research-notes/
docs/release/
docs/DEVELOPMENT_GUIDE.md
docs/INTEGRATION_GUIDE.md

# 测试 & 构建过程产物
__tests__/
*.test.ts
*.spec.ts
tsup.config.ts
tsconfig.json
.npmrc
.npmrc.example
.eslintrc*
vitest.config.*

# 节点工具默认黑名单
node_modules/
coverage/
*.log
.DS_Store
