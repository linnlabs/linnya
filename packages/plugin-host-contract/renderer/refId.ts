export declare function generateRefMap(ids: readonly string[]): Promise<Map<string, string>>;
export declare function isValidRef(ref: string): boolean;
export declare function normalizeRef(ref: string): string;
