export const LINNYA_PLUGIN_CLI_COMMAND_MODE_MARKER = '--linnya-plugin-cli';
export const LINNYA_PLUGIN_ROOT_ENV = 'LINNYA_PLUGIN_ROOT';
export const LINNYA_PLUGIN_RUNTIME_DATABASE_PATH_ENV = 'LINNYA_PLUGIN_RUNTIME_DATABASE_PATH';
/**
 * Shell 子进程可用的当前对话根目录。它只存在于后端 launch environment，
 * 不进入命令 proposal、事件、Renderer 或 Agent tool 参数。
 */
export const LINNYA_CONVERSATION_ROOT_ENV = 'LINNYA_CONVERSATION_ROOT';
