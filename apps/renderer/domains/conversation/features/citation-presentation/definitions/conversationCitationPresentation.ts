import type { ConversationCitationWorkspace } from '@linnya/citation-domain/conversation-presentation';

/** Renderer 命名只表达该工作区在 MessageProjection 内的用途，不复制 Citation domain 合同。 */
export type ConversationCitationProjectionWorkspace = ConversationCitationWorkspace;
