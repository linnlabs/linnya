import type { Editor } from '@tiptap/core';

export function mergeTableCellsCustom(editor: Editor | null | undefined): boolean;

export function mergeCells(editor: Editor | null | undefined): boolean;

export function splitCell(editor: Editor | null | undefined): boolean;
